<view class="team-page">
  <view class="search-box">
    <input class="search-input" placeholder="请输入查询医生" value="{{searchKeyword}}" bindinput="handleInput" bindconfirm="handleSearch" confirm-type="search" />
    <button class="search-btn" bindtap="handleSearch">搜索</button>
  </view>

  <block wx:for="{{doctors}}" wx:key="name">
    <view class="doctor-card">
      <view class="doctor-avatar" style="background: {{item.avatarBg || '#cfe2ff'}}">{{item.name.slice(0,1)}}</view>
      <view class="doctor-content">
        <view class="doctor-header">
          <view>
            <view class="doctor-name">{{item.name}}</view>
            <view class="doctor-title">{{item.title}}</view>
          </view>
          <button class="book-btn" data-name="{{item.name}}" bindtap="openDoctor">预约</button>
        </view>
        <view class="doctor-meta">{{item.expertise}}</view>
        <view class="doctor-intro">【擅长】{{item.intro}}</view>
      </view>
    </view>
  </block>

  <empty-state wx:if="{{!doctors.length}}" text="暂无医生信息" icon="🩺" />
</view>
